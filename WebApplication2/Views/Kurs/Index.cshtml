@model IEnumerable<WebApplication2.Models.Kurs>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Grafik)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Grafik.ID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Kod">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Kod">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Kod">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
