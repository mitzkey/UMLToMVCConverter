@model IEnumerable<WebApplication2.Models.CzlonekKlubu>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.WniosekPrzyjetyNaPodstawie)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.WniosekPrzyjetyNaPodstawie.Pesel)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Pesel">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Pesel">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Pesel">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
