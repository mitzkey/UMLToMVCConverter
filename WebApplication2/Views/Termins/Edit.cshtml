@model WebApplication2.Models.Termin

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>Termin</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="GrafikID" class="control-label"></label>
                <select asp-for="GrafikID" class="form-control" asp-items="ViewBag.GrafikID"></select>
                <span asp-validation-for="GrafikID" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="Dzien" />
            <input type="hidden" asp-for="GodzinaRozpoczecia" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
