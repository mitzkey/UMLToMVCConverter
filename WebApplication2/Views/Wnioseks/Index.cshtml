@model IEnumerable<WebApplication2.Models.Wniosek>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.DataZlozenia)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.DataRozpatrzenia)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.AdresDoKorespondencji)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AdresZameldowania)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DataZlozenia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataRozpatrzenia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AdresDoKorespondencji.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AdresZameldowania.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status.ID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Pesel">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Pesel">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Pesel">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
